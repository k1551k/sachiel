{
  "name": "sachiel",
  "scripts": {
    "dev": "node --env-file=.env.dev --watch src/index.js",
    "start": "pm2 start ecosystem.config.js",
    "pull": "git pull origin main && pm2 restart sachiel"
  },
  "packageManager": "pnpm@10.12.4",
  "dependencies": {
    "express": "^5.1.0",
    "ky": "^1.8.2"
  }
}
